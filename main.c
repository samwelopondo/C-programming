#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    printf("Sizeof char is %ld bytes\n",sizeof(char));
    printf("Sizeof int is %ld bytes\n",sizeof(int));
    return (1);
}
